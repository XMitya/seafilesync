# Robolectric 4.16.1 ships Android images up to SDK 36, while the project targets 37.
# Without this it refuses to start: "Package targetSdkVersion=37 > maxSdkVersion=36".
# Raise this once Robolectric publishes a 37 image.
sdk=36
