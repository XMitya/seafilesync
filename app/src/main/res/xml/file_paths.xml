<?xml version="1.0" encoding="utf-8"?>
<!-- Only the exported log is shareable. Nothing else the app holds should leave it this way. -->
<paths>
    <cache-path name="diagnostics" path="." />
</paths>
